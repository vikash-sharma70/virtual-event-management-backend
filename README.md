# virtual-event-management-backend
Backend system for a virtual event management platform built with Node.js and Express.js. Features secure JWT authentication, bcrypt password hashing, event CRUD operations, participant registration, email notifications, and RESTful APIs using in-memory data structures for managing users and events.
